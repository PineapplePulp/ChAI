use Tensor;


proc main(args: [] string) {
    compilerError("WebcamFilter: This demo is not yet implemented.");
    // const a = Bridge.captureWebcam(0);

    // writeln(a.type:string);
    // const b = Bridge.bridgeTensorToArray(3,a);
    // writeln(b.shape);
    // writeln(b);

}